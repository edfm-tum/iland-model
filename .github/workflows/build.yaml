name: Qt qmake Cross-Platform Build

on:
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      build_type:
        description: 'manual build'
        required: false
        default: 'Manual Dev Build'
  push:
    tags:
      - 'v*'

jobs:
  build:
    name: Build on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Install Qt
        uses: jurplel/install-qt-action@v3
        with:
          version: '6.8.0'
          # qtcharts necessary to be installed extra
          modules: 'qtcharts'
          # specific toolchain for windows
          arch: ${{ matrix.os == 'windows-latest' && 'win64_msvc2022_64' || '' }}

      # --- dependencies ---
      - name: Install FreeImage (Linux)
        if: runner.os == 'Linux'
        run: sudo apt-get update && sudo apt-get install -y libfreeimage-dev libxcb-cursor0

      - name: Install FreeImage (macOS)
        if: runner.os == 'macOS'
        run: brew install freeimage

      - name: Set up MSVC
        if: matrix.os == 'windows-latest'
        uses: ilammy/msvc-dev-cmd@v1

      # --- Build Phase ---
      - name: Build Project (Windows)
        if: matrix.os == 'windows-latest'
        shell: pwsh
        run: |
          # runs windows build script and creates a log file
          ./build_iland.ps1 2>&1 | Tee-Object -FilePath build_log.txt

      - name: Build Project (Linux/macOS)
        if: matrix.os != 'windows-latest'
        shell: bash
        run: |
          # chmod to make sure it is executable
          chmod +x ./build_iland.sh
          ./build_iland.sh 2>&1 | tee build_log.txt

      # --- Deployment Phase ---

      # 1. Windows
      - name: Windows Deployment
        if: matrix.os == 'windows-latest'
        shell: pwsh
        run: |
          mkdir deploy-win
          copy src/iland/build/release/release/iland.exe deploy-win/
          copy src/ilandc/build/release/release/ilandc.exe deploy-win/
          copy src/fonstudio/build/release/release/fonstudio.exe deploy-win/
          copy libs/win/FreeImage.dll deploy-win/
          windeployqt --release --no-translations --compiler-runtime --qmldir src/iland/qml deploy-win/iland.exe
          # copy log
          move build_log.txt deploy-win/

      # 2. Linux Deployment (AppImage)
      - name: Linux Deployment
        if: matrix.os == 'ubuntu-latest'
        run: |
          # 1. Verzeichnisse vorbereiten
          mkdir -p deploy-linux

          # 2. linuxdeploy und das Qt-Plugin herunterladen
          wget -c https://github.com/linuxdeploy/linuxdeploy/releases/download/continuous/linuxdeploy-x86_64.AppImage
          wget -c https://github.com/linuxdeploy/linuxdeploy-plugin-qt/releases/download/continuous/linuxdeploy-plugin-qt-x86_64.AppImage
          chmod +x linuxdeploy*.AppImage

          # 3. Umgebungsvariablen setzen
          # Wir müssen linuxdeploy sagen, wo qmake liegt und dass wir QML nutzen
          export QMAKE=$(which qmake)
          export QML_SOURCES_PATHS=src/iland/qml

          # 4. AppImage erstellen
          # -e: Pfad zum Executable
          # --plugin qt: Aktiviert das Qt-Plugin
          # --output appimage: Erzeugt die finale .AppImage Datei
          ./linuxdeploy-x86_64.AppImage --appdir AppDir -e src/iland/build/release/iland --plugin qt --output appimage

          # 5. Alles für den Upload einsammeln
          mv *.AppImage deploy-linux/
          cp src/ilandc/build/release/ilandc deploy-linux/ # Konsolentool separat
          cp build_log.txt deploy-linux/

      # 3. macOS
      - name: macOS Deployment
        if: matrix.os == 'macos-latest'
        run: |
          mkdir deploy-mac
          cp -r src/iland/build/release/iland.app deploy-mac/
          cp -r src/ilandc/build/release/ilandc deploy-mac/
          # bundle everything into an app
          macdeployqt deploy-mac/iland.app -qmldir=src/iland/qml
          cp build_log.txt deploy-mac/

      # --- Artifact Upload ---
      - name: Upload Build Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: iLand-Build-${{ matrix.os }}
          path: |
            deploy-win/
            deploy-linux/
            deploy-mac/
